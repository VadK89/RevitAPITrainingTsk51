<Window x:Class="RevitAPITrainingTsk51.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RevitAPITrainingTsk51"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="200"
    Title="Идентификатор элемента">
    <StackPanel>

        <Button Content="Количество труб" Command="{Binding NumberPipesCommand}"  Margin="5"/>
        <Button Content="Объёмом всех стен" Command="{Binding VolumeWallCommand}"  Margin="5"/>
        <Button Content="Количество дверей" Command="{Binding NumberDoorCommand}" Margin="5"/>
    </StackPanel>
</Window>
